import React from "react";
import { Task } from "../../features/tasks/Task";

interface TaskCardProps {
  taskData: Task;
}

export default function TaskCard({ taskData }: TaskCardProps) {
  return <div></div>;
}
